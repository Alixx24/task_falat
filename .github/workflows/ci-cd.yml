name: Laravel CI/CD

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: testdb
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      # 1. Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Setup PHP
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          extensions: mbstring, bcmath, mysqli, pdo_mysql, curl, xml
          ini-values: post_max_size=256M, max_execution_time=180

      # 3. Install Composer dependencies
      - name: Install Composer dependencies
        working-directory: ./Task
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      # 4. Run Laravel tests
      - name: Run Laravel tests
        working-directory: ./Task
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: testdb
          DB_USERNAME: root
          DB_PASSWORD: root
        run: php artisan test --parallel

      # 5. Deploy to Server
      - name: Deploy to Server
        if: github.ref == 'refs/heads/main'
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          # انتقال Laravel به سرور
          rsync -avz --delete ./Task/ $REMOTE_USER@$REMOTE_HOST:/var/www/laravel-app/
          # اجرای Migration
          ssh $REMOTE_USER@$REMOTE_HOST "cd /var/www/laravel-app && php artisan migrate --force"
